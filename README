>Развёртывание базы
1. Установить `Docker`
2. После установки запустить профиль `Run bd`

>Запуск сервера
1. Выполнить команду в терминале ```pip install -r requirements.txt```
2. Запустить профиль `Flask(app.py)`